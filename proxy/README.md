# ReeR HTTP Proxy - Простой прокси для API запросов

**Простое, быстрое и надежное решение** для проксирования HTTP/HTTPS запросов к OpenAI, Claude и другим AI сервисам из России.

## 🎯 Для чего это решение

Если у вас:
- ✅ Проект хостится на **российском сервере**
- ✅ Нужно делать запросы к **OpenAI/Claude/другим AI API**
- ✅ Много **параллельных запросов** (10-100+)
- ✅ Нужна **высокая скорость** и **стабильность**

То это решение **именно для вас**! Не нужен VPN клиент на компьютере или телефоне - просто HTTP прокси.

## ⚡ Преимущества перед VPN

| Параметр | HTTP Proxy (это решение) | VPN (старое решение) |
|----------|-------------------------|---------------------|
| **Скорость** | ⚡ Очень быстро | 🐌 Медленно (туннелирование) |
| **Стабильность** | ✅ Высокая | ⚠️ Может рвать соединение |
| **Настройка** | 1 команда | Сложная настройка клиентов |
| **Нагрузка** | 100+ параллельных запросов | Проблемы при высокой нагрузке |
| **Использование** | Указать прокси в коде | Нужен VPN клиент |

## 🚀 Быстрый старт

### 1. На вашем зарубежном сервере (например Hetzner)

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/ReeR.git
cd ReeR/proxy

# Запустите прокси
./deploy.sh
```

Готово! Прокси сервер запущен на порту **3128**.

### 2. В вашем приложении на российском сервере

#### Node.js (Express/NestJS/любой фреймворк)

```bash
npm install axios https-proxy-agent
```

```javascript
const axios = require('axios');
const { HttpsProxyAgent } = require('https-proxy-agent');

// Укажите IP вашего прокси сервера
const httpsAgent = new HttpsProxyAgent('http://YOUR_SERVER_IP:3128');

// Все запросы к OpenAI теперь идут через прокси
const response = await axios.post(
  'https://api.openai.com/v1/chat/completions',
  {
    model: 'gpt-4',
    messages: [{ role: 'user', content: 'Hello!' }]
  },
  {
    httpsAgent,
    headers: {
      'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`
    }
  }
);
```

#### Python (FastAPI/Django/Flask)

```bash
pip install requests
```

```python
import requests

# Укажите IP вашего прокси сервера
proxies = {
    "http": "http://YOUR_SERVER_IP:3128",
    "https": "http://YOUR_SERVER_IP:3128"
}

# Все запросы к OpenAI теперь идут через прокси
response = requests.post(
    "https://api.openai.com/v1/chat/completions",
    json={
        "model": "gpt-4",
        "messages": [{"role": "user", "content": "Hello!"}]
    },
    proxies=proxies,
    headers={
        "Authorization": f"Bearer {os.getenv('OPENAI_API_KEY')}"
    }
)
```

## 📁 Структура

```
proxy/
├── docker-compose.yml      # Docker Compose конфигурация
├── squid.conf              # Конфигурация Squid (оптимизирована)
├── deploy.sh               # Скрипт быстрого деплоя
├── README.md               # Эта документация
└── examples/               # Примеры использования
    ├── nodejs-axios.js     # Пример для Node.js
    ├── python-requests.py  # Пример для Python
    ├── package.json        # Dependencies для Node.js
    └── requirements.txt    # Dependencies для Python
```

## 🔧 Управление

### Запуск
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Перезапуск
```bash
docker-compose restart
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Проверка статуса
```bash
docker ps | grep reer-proxy
```

## 🎯 Примеры использования

В папке `examples/` есть полные рабочие примеры:

### Node.js
```bash
cd examples
npm install
# Отредактируйте nodejs-axios.js - укажите ваш IP
node nodejs-axios.js
```

### Python
```bash
cd examples
pip install -r requirements.txt
# Отредактируйте python-requests.py - укажите ваш IP
python python-requests.py
```

## ⚙️ Оптимизация производительности

Текущая конфигурация оптимизирована для:
- ✅ **100+ параллельных запросов**
- ✅ **4 воркера** (для многоядерных процессоров)
- ✅ **Большие ответы** (до 100MB)
- ✅ **Длительные запросы** (до 180 сек таймаут)
- ✅ **65536 файловых дескрипторов**

Если нужно больше - отредактируйте `squid.conf`:
```conf
workers 8                    # Увеличить количество воркеров
max_filedescriptors 131072   # Больше соединений
```

## 🔒 Безопасность (опционально)

### Добавить аутентификацию

1. Установите htpasswd:
```bash
apt-get install apache2-utils
```

2. Создайте пароль:
```bash
htpasswd -c ./passwords username
```

3. Раскомментируйте строки в `squid.conf`:
```conf
auth_param basic program /usr/lib/squid/basic_ncsa_auth /etc/squid/passwords
auth_param basic realm Proxy Authentication
acl authenticated proxy_auth REQUIRED
http_access allow authenticated
http_access deny all
```

4. Добавьте volume в `docker-compose.yml`:
```yaml
volumes:
  - ./passwords:/etc/squid/passwords:ro
```

5. Используйте в коде:
```javascript
// Node.js
const proxy = 'http://username:password@YOUR_SERVER_IP:3128';

// Python
proxies = {
    "https": "http://username:password@YOUR_SERVER_IP:3128"
}
```

### Ограничить доступ по IP

В `squid.conf` добавьте:
```conf
acl allowed_ips src 123.456.789.0/24  # Ваша подсеть
http_access allow allowed_ips
http_access deny all
```

## 🐛 Решение проблем

### Прокси не запускается
```bash
# Проверьте логи
docker-compose logs

# Проверьте порт 3128
netstat -tulpn | grep 3128

# Проверьте firewall
ufw allow 3128
```

### Медленные запросы
```bash
# Проверьте использование ресурсов
docker stats reer-proxy

# Увеличьте лимиты памяти в docker-compose.yml
```

### Ошибки соединения
```bash
# Проверьте что прокси доступен
curl -x http://localhost:3128 https://api.openai.com/v1/models

# Проверьте DNS
docker exec reer-proxy cat /etc/resolv.conf
```

## 📊 Мониторинг

### Просмотр статистики
```bash
# Количество запросов
docker-compose logs | grep "TCP_TUNNEL" | wc -l

# Последние запросы
docker-compose logs --tail=50
```

### Кэш статистика (если включите кэш)
```bash
docker exec reer-proxy squidclient mgr:info
```

## 💡 Сравнение с альтернативами

| Решение | Сложность | Скорость | Надежность | Стоимость |
|---------|-----------|----------|------------|-----------|
| **ReeR Proxy** | ⭐ Легко | ⚡⚡⚡ Быстро | ✅ Высокая | 💰 $4-6/мес |
| VPN (Xray/WireGuard) | ⭐⭐⭐ Сложно | ⚡ Медленно | ⚠️ Средняя | 💰 $4-6/мес |
| SOCKS5 Proxy | ⭐⭐ Средне | ⚡⚡ Средне | ✅ Высокая | 💰 $4-6/мес |
| Платный прокси | ⭐ Легко | ⚡⚡ Средне | ⚠️ Зависит | 💰💰 $20+/мес |

## ❓ FAQ

**Q: Можно ли использовать для телефона/компьютера?**
Да, но это не оптимально. Для этого лучше использовать VPN решение из корневой папки проекта.

**Q: Сколько запросов выдержит?**
На 1 CPU / 512MB RAM - **100-200** параллельных запросов. На 2 CPU / 1GB - **500+**.

**Q: Работает ли для других API (не OpenAI)?**
Да! Работает для любых HTTP/HTTPS запросов: Claude, Gemini, любые API.

**Q: Нужен ли домен?**
Нет, достаточно IP адреса сервера.

**Q: Безопасно ли?**
Да, весь трафик между вашим сервером и прокси шифруется HTTPS. Добавьте аутентификацию для дополнительной безопасности.

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если что-то не работает - создайте Issue в репозитории.

---

**⚠️ Важно:** Это решение предназначено **только для проксирования API запросов** из вашего приложения. Для VPN на телефоне/компьютере используйте основное VPN решение из корня репозитория.
