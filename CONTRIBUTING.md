# 🤝 Участие в разработке

Спасибо за интерес к проекту ReeR VPN! Мы рады любому вкладу.

## 🎯 Как помочь проекту

### 1. Сообщайте об ошибках

Нашли баг? Создайте Issue:

1. Проверьте что похожего Issue еще нет
2. Запустите диагностику: `./scripts/troubleshoot.sh`
3. Создайте Issue с:
   - Описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Вывод troubleshoot.sh
   - Ваша ОС и версия Docker

### 2. Предлагайте улучшения

Есть идея? Создайте Feature Request:

1. Опишите проблему которую решает фича
2. Опишите желаемое решение
3. Опишите альтернативы которые вы рассматривали
4. Дополнительный контекст (скриншоты, примеры)

### 3. Улучшайте документацию

Документация никогда не бывает идеальной:

- Исправьте опечатки
- Добавьте примеры
- Улучшите объяснения
- Переведите на другие языки

### 4. Пишите код

Хотите добавить функционал? Отлично!

## 📋 Процесс разработки

### Шаг 1: Fork репозитория

```bash
# На GitHub нажмите Fork
# Затем клонируйте ваш fork
git clone https://github.com/YOUR_USERNAME/ReeR.git
cd ReeR
```

### Шаг 2: Создайте ветку

```bash
# Создайте ветку от main
git checkout -b feature/my-awesome-feature

# Или для багфикса
git checkout -b fix/some-bug
```

### Шаг 3: Внесите изменения

```bash
# Редактируйте файлы
# Тестируйте изменения
./setup.sh  # Проверьте что установка работает
```

### Шаг 4: Commit

```bash
# Добавьте изменения
git add .

# Создайте commit с понятным сообщением
git commit -m "feat: add user auto-renewal feature"

# Или для багфикса
git commit -m "fix: resolve port conflict issue"
```

**Формат commit messages:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование (не влияет на код)
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигов

### Шаг 5: Push и Pull Request

```bash
# Push в ваш fork
git push origin feature/my-awesome-feature
```

На GitHub:
1. Откройте ваш fork
2. Нажмите "Compare & pull request"
3. Заполните описание PR:
   - Что изменено
   - Зачем это нужно
   - Как тестировали

## 🎨 Стандарты кода

### Bash скрипты

```bash
#!/bin/bash

# Используйте set -e для остановки при ошибках
set -e

# Комментируйте неочевидный код
# Это делает то-то и то-то
some_command

# Используйте понятные имена переменных
SERVER_IP=$(curl -s ifconfig.me)
BACKUP_DIR="backups"

# Проверяйте входные данные
if [[ -z "$1" ]]; then
    echo "Ошибка: укажите параметр"
    exit 1
fi

# Используйте функции для повторяющегося кода
print_success() {
    echo -e "${GREEN}✅ $1${NC}"
}
```

### Docker Compose

```yaml
# Комментируйте неочевидные настройки
services:
  service-name:
    # Используйте официальные образы
    image: official/image:latest

    # Всегда указывайте restart policy
    restart: unless-stopped

    # Группируйте связанные настройки
    environment:
      - VAR1=value1
      - VAR2=value2
```

### Markdown документация

```markdown
# Заголовок H1 - только один на файл

## Заголовок H2 - для секций

### Заголовок H3 - для подсекций

- Используйте списки для перечислений
- Короткие абзацы (2-4 предложения)
- Примеры кода с подсветкой синтаксиса

\`\`\`bash
# Блоки кода с языком
command --option value
\`\`\`

**Жирный** для важных терминов
*Курсив* для акцента
`код` для команд и файлов
```

## ✅ Checklist для PR

Перед созданием PR проверьте:

- [ ] Код работает и протестирован
- [ ] Добавлены комментарии к неочевидным частям
- [ ] Обновлена документация (если нужно)
- [ ] Нет конфликтов с main веткой
- [ ] Commit messages следуют формату
- [ ] Скрипты имеют права на выполнение (`chmod +x`)
- [ ] Нет секретных данных (пароли, ключи, IP)
- [ ] Файлы оканчиваются переводом строки

## 🧪 Тестирование

### Локальное тестирование

```bash
# Запустите установку в Docker контейнере
docker run -it --rm \
  -v $(pwd):/root/ReeR \
  ubuntu:22.04 \
  bash -c "cd /root/ReeR && ./setup.sh"

# Или на виртуальной машине
# Используйте Vagrant или VirtualBox
```

### Проверка скриптов

```bash
# Shellcheck для bash скриптов
shellcheck setup.sh
shellcheck scripts/*.sh

# Проверка YAML
docker compose config
```

## 📝 Примеры хороших PR

### Хороший PR

**Заголовок:** `feat: add automatic backup to cloud storage`

**Описание:**
```
## Что изменено
- Добавлен скрипт backup-cloud.sh для бэкапа в S3/GCS
- Обновлена документация с инструкциями по настройке
- Добавлен пример cron задачи

## Зачем
Пользователи просили возможность автоматического бэкапа в облако (#42)

## Как тестировал
- Протестировал на AWS S3
- Протестировал на Google Cloud Storage
- Добавил проверку ошибок и retry логику
```

### Плохой PR

**Заголовок:** `Update`

**Описание:**
```
fixed stuff
```

❌ Непонятно что изменено и зачем

## 🔒 Безопасность

### Сообщение об уязвимостях

**НЕ создавайте публичные Issues для уязвимостей!**

Вместо этого:
1. Напишите на email: security@reer-vpn.example.com (замените на реальный)
2. Опишите уязвимость
3. Приложите PoC (если есть)
4. Дайте 30 дней на исправление

### Безопасность кода

- ✅ Никогда не коммитьте пароли, ключи, токены
- ✅ Используйте переменные окружения для секретов
- ✅ Проверяйте входные данные
- ✅ Используйте официальные Docker образы
- ✅ Обновляйте зависимости

## 🌍 Локализация

Хотите перевести на свой язык?

1. Создайте директорию `docs/LANG/` (например, `docs/ru/`)
2. Переведите основные файлы:
   - README.md
   - QUICKSTART.md
   - FAQ.md
3. Создайте PR

## 💬 Где обсудить?

- **GitHub Discussions** - общие вопросы
- **GitHub Issues** - баги и фичи
- **Pull Requests** - обсуждение кода

## 📜 Лицензия

Внося вклад, вы соглашаетесь что ваш код будет лицензирован под MIT License.

## 🙏 Благодарности

Спасибо всем кто вносит вклад в проект!

Список контрибьюторов: https://github.com/YOUR_USERNAME/ReeR/graphs/contributors

---

**Вопросы?** Создайте Discussion на GitHub!

**Спасибо за помощь!** ❤️
