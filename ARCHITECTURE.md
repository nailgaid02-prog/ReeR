# 🏗️ Архитектура ReeR VPN

Подробное описание архитектуры проекта.

## 📐 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Клиенты (РФ)                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Телефон  │  │ Телефон  │  │  Ноутбук │  │  Сервер  │   │
│  │   #1     │  │   #2     │  │          │  │  (проект)│   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
└───────┼─────────────┼─────────────┼─────────────┼──────────┘
        │             │             │             │
        │    Зашифрованный трафик (VLESS/VMess)  │
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              ReeR VPN Server (EU/US)                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Docker Host (Ubuntu)                      │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         3x-ui Container                         │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  Web Panel (Port 54321)                   │  │  │  │
│  │  │  │  - User Management                        │  │  │  │
│  │  │  │  - Config Generation                      │  │  │  │
│  │  │  │  - Statistics                             │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  Xray-core                                │  │  │  │
│  │  │  │  - VLESS (Port 443)                       │  │  │  │
│  │  │  │  - VMess (Port 2053)                      │  │  │  │
│  │  │  │  - Routing Engine                         │  │  │  │
│  │  │  │  - Traffic Processing                     │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  SQLite Database                          │  │  │  │
│  │  │  │  - Users                                  │  │  │  │
│  │  │  │  - Configs                                │  │  │  │
│  │  │  │  - Statistics                             │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │      Watchtower Container                       │  │  │
│  │  │      - Auto-updates                             │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │      UFW Firewall                               │  │  │
│  │  │      - Port 22 (SSH)                            │  │  │
│  │  │      - Port 443 (VLESS)                         │  │  │
│  │  │      - Port 2053 (VMess)                        │  │  │
│  │  │      - Port 54321 (Panel)                       │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │ Расшифрованный трафик
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    Интернет                                 │
│  YouTube │ Instagram │ OpenAI │ Claude │ Payment APIs      │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Поток данных

### 1. Подключение клиента

```
Client                    Server                    Internet
  │                         │                          │
  ├─[1] Connect (VLESS)────>│                          │
  │                         ├─[2] Validate UUID        │
  │                         ├─[3] Establish TLS        │
  │<─[4] Connection OK──────┤                          │
  │                         │                          │
```

### 2. Обработка трафика

```
Client                    Xray-core                 Destination
  │                         │                          │
  ├─[1] Request (enc)──────>│                          │
  │                         ├─[2] Decrypt              │
  │                         ├─[3] Route                │
  │                         ├─[4] Request─────────────>│
  │                         │<─[5] Response────────────┤
  │                         ├─[6] Encrypt              │
  │<─[7] Response (enc)─────┤                          │
  │                         │                          │
```

### 3. Reality Protocol (VLESS)

```
Observer                  Client                    Server
  │                         │                          │
  │<───[Looks like]─────────┤                          │
  │    HTTPS to             ├──[Encrypted Tunnel]────>│
  │    real website         │                          │
  │                         │                          │
  └─Cannot detect VPN       └─Real VPN connection─────┘
```

## 🗂️ Структура проекта

```
ReeR/
├── 📄 Configuration Files
│   ├── docker-compose.yml          # Docker services definition
│   ├── .gitignore                  # Git ignore rules
│   └── LICENSE                     # MIT License
│
├── 📜 Installation Scripts
│   ├── setup.sh                    # Main installation script
│   └── install-remote.sh           # Remote installation (curl | bash)
│
├── 🛠️ Utility Scripts
│   └── scripts/
│       ├── status.sh               # System status check
│       ├── backup.sh               # Backup configuration
│       ├── update.sh               # Update containers
│       ├── troubleshoot.sh         # Diagnostic tool
│       └── enable-bbr.sh           # Enable BBR TCP optimization
│
├── 📚 Documentation
│   ├── README.md                   # Main documentation
│   ├── QUICKSTART.md               # 5-minute quick start
│   ├── DEPLOY.md                   # Deployment guides
│   ├── SECURITY.md                 # Security audit
│   ├── FAQ.md                      # Frequently asked questions
│   ├── ARCHITECTURE.md             # This file
│   ├── CONTRIBUTING.md             # Contribution guidelines
│   └── CHANGELOG.md                # Version history
│
└── 📦 Data (created on install, gitignored)
    └── data/
        ├── db/                     # SQLite database
        ├── logs/                   # Application logs
        └── cert/                   # SSL certificates
```

## 🐳 Docker Architecture

### Containers

**3x-ui Container:**
- **Image:** `ghcr.io/mhsanaei/3x-ui:latest`
- **Network Mode:** `host` (для прямого доступа к портам)
- **Volumes:**
  - `./data/db:/etc/x-ui` - база данных
  - `./data/logs:/var/log/3x-ui` - логи
  - `./data/cert:/root/cert` - сертификаты
- **Restart Policy:** `unless-stopped`

**Watchtower Container:**
- **Image:** `containrrr/watchtower:latest`
- **Purpose:** Автоматическое обновление контейнеров
- **Schedule:** Каждые 24 часа
- **Volumes:**
  - `/var/run/docker.sock` - Docker socket для управления

### Networks

**Host Network Mode:**
- Используется для 3x-ui для прямого доступа к портам
- Упрощает конфигурацию без NAT
- Производительность выше (нет network overlay)

## 🔐 Безопасность

### Уровни защиты

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: Network Firewall (UFW)                             │
│ - Блокирует все кроме разрешенных портов                    │
│ - Rate limiting                                              │
└───────────────────────┬─────────────────────────────────────┘
                        │ Allowed ports only
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: TLS Encryption                                      │
│ - TLS 1.3 (самая современная версия)                        │
│ - AES-256-GCM / ChaCha20-Poly1305                           │
│ - Perfect Forward Secrecy                                    │
└───────────────────────┬─────────────────────────────────────┘
                        │ Encrypted connection
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: Authentication                                      │
│ - UUID-based client authentication                          │
│ - Web panel password                                         │
│ - Optional IP whitelist                                      │
└───────────────────────┬─────────────────────────────────────┘
                        │ Authenticated user
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 4: Application Logic                                  │
│ - Traffic routing                                            │
│ - User quotas                                                │
│ - Statistics                                                 │
└─────────────────────────────────────────────────────────────┘
```

### Протоколы безопасности

**VLESS + Reality:**
```
Client ──[TLS to real site]──> Observer: "Looks normal"
Client ══[Hidden VPN tunnel]═> Server:  "Real connection"
```

**VMess + WebSocket:**
```
Client ──[WebSocket (HTTPS)]──> Observer: "Web traffic"
Client ══[Obfuscated data]═══> Server:  "VPN data"
```

## ⚡ Оптимизации

### BBR TCP Congestion Control

```
Standard TCP              BBR TCP
    │                        │
    ├─ Slow start           ├─ Fast start
    ├─ Conservative         ├─ Aggressive
    ├─ Packet loss = slow   ├─ Packet loss handled
    │                        │
Result: Slower           Result: 2-5x faster
```

**Включение:**
```bash
# Автоматически в setup.sh
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
```

### Connection Pooling

Xray-core переиспользует соединения:
- Меньше handshakes
- Меньше latency
- Выше throughput

### Zero-Copy

Xray использует zero-copy где возможно:
- sendfile() syscall
- splice() for sockets
- Меньше CPU usage

## 📊 Масштабируемость

### Одиночный сервер

**Specs:** 1 vCPU, 1GB RAM
**Capacity:**
- 5-10 одновременных пользователей
- ~100 Mbps throughput
- ~1TB/month трафик

**Specs:** 2 vCPU, 2GB RAM
**Capacity:**
- 20-30 одновременных пользователей
- ~500 Mbps throughput
- ~5TB/month трафик

**Specs:** 4 vCPU, 8GB RAM
**Capacity:**
- 100+ одновременных пользователей
- ~1 Gbps throughput
- ~20TB/month трафик

### Множественные серверы

Для больших нагрузок:

```
           Load Balancer (DNS Round Robin)
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
   Server 1     Server 2     Server 3
   (EU West)    (EU East)    (US East)
```

**Setup:**
1. Разверните ReeR на нескольких серверах
2. Дайте каждому свой домен (vpn1.domain.com, vpn2.domain.com)
3. Настройте DNS Round Robin или используйте Cloudflare Load Balancing
4. Распределите пользователей по серверам

## 🔍 Мониторинг

### Встроенный (3x-ui Panel)

- Traffic statistics per user
- Online users count
- System resources (CPU, RAM, Disk)
- Connection logs

### Расширенный (опционально)

**Prometheus + Grafana:**

```yaml
# Добавить в docker-compose.yml
prometheus:
  image: prom/prometheus
  volumes:
    - ./prometheus.yml:/etc/prometheus/prometheus.yml

grafana:
  image: grafana/grafana
  ports:
    - "3000:3000"
```

**Metrics:**
- Bandwidth per user
- Connection duration
- Error rates
- Geographic distribution

## 🔄 Обновления

### Автоматические (Watchtower)

```
Every 24h:
  Check for new images
    ↓
  New version available?
    ↓ Yes
  Pull new image
    ↓
  Stop old container
    ↓
  Start new container
    ↓
  Remove old image
```

### Ручные

```bash
./scripts/update.sh
  ↓
Create backup
  ↓
Pull new images
  ↓
Restart containers
  ↓
Verify status
```

## 🛡️ Disaster Recovery

### Backup Strategy

**Что бэкапится:**
- SQLite database (пользователи, конфиги)
- SSL сертификаты
- docker-compose.yml
- Логи установки

**Где хранить:**
- Локально: `./backups/`
- Удаленно: rclone → Google Drive/S3/Dropbox

**Расписание:**
```cron
# Каждую неделю в 3:00
0 3 * * 0 cd /root/ReeR && ./scripts/backup.sh
```

### Восстановление

```bash
# 1. Новый сервер
ssh root@new-server

# 2. Установка ReeR
curl -fsSL https://...install-remote.sh | bash

# 3. Остановка
docker compose down

# 4. Восстановление данных
scp backup.tar.gz root@new-server:/root/ReeR/
tar -xzf backup.tar.gz

# 5. Запуск
docker compose up -d
```

## 📈 Производительность

### Бенчмарки

**VLESS + Reality:**
- Latency overhead: +2-5ms
- Throughput: 95-98% of bare metal
- CPU usage: Low (hardware AES)

**VMess + WebSocket:**
- Latency overhead: +5-10ms
- Throughput: 90-95% of bare metal
- CPU usage: Medium

### Оптимизация

**1. Enable BBR:**
```bash
./scripts/enable-bbr.sh
# Improvement: +50-200% throughput
```

**2. Use VLESS instead of VMess:**
```
VLESS: Less overhead, faster
VMess: More overhead, slower
```

**3. Optimize MTU:**
```
Standard: 1500
Optimized: 1400-1420 (меньше fragmentation)
```

## 🔮 Будущие улучшения

### Запланировано

- [ ] WebUI на русском языке
- [ ] Telegram бот для управления
- [ ] Автоматическое определение оптимального протокола
- [ ] Multi-hop routing (chain proxies)
- [ ] Built-in DNS over HTTPS
- [ ] IPv6 support
- [ ] Kubernetes helm chart
- [ ] Мобильное приложение

### Идеи

- API для программного управления
- Payment gateway integration
- Referral system
- Custom domains per user
- Traffic shaping
- DDoS protection

---

**Вопросы по архитектуре?** Создайте Discussion на GitHub!
